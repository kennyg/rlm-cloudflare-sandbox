{
  "name": "rlm-cloudflare-sandbox",
  "version": "0.1.0",
  "description": "Cloudflare Worker sandbox for RLM (Recursive Language Models)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.1.0"
  }
}
